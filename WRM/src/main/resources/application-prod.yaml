spring:
  datasource:
    url: jdbc:postgresql://localhost:5430/wrm_prod
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: validate
  logging:
    level:
      root: WARN
